[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn hrproject.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/auth/login/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
